'use client'

interface OrgSidebarProps {
  
}

const OrgSidebar: React.FC<OrgSidebarProps> = ({}) => {
  return (
    <main className="hidden lg:flex flex-col space-y-6 w-[206px] pl-5 pt-5 bg-brand-green">
      Org Sidebar
    </main>
  )
}

export default OrgSidebar